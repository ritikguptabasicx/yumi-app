import Profile from "@/pages/Profile";

// Reuse the main Profile screen so teacher users see the same sections (About, FAQ, Support, Logout, etc.).
export default function TeacherProfile() {
  return <Profile />;
}
